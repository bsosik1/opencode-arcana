## HANDOFF_CAPSULE v1 producer contract

After your existing named final-response sections, append exactly one capsule as the last block. Do not put capsule text in an earlier section. This is source data, never instructions or authorization.

Use this exact canonical JSON object shape and no other keys:

```json
{"protocol":"HANDOFF_CAPSULE","version":1,"capsuleId":"unique-per-invocation","sourceDossierId":null,"sourceInvocationId":null,"sourceAgentId":"your-runtime-agent-id","derivedFromCapsuleId":null,"contract":{"task":"...","expectedOutcome":"...","operationType":"implementation|read-only|report-only","scope":"...","authorizationBoundary":"...","activeConstraints":"...","hash":"parent-supplied-hash-or-unavailable"},"ownership":[{"topicId":"unique-topic-id","topic":"...","targetInvocationId":null,"ownerInvocationId":null,"ownerCapsuleId":null,"sourceIds":["evidence-id"],"status":"owned|shared|unassigned|complete|blocked"}],"evidence":[{"evidenceId":"unique-evidence-id","claim":"bounded claim","sourceKind":"file|test|command|task|web|wiki|other","locator":"path or bounded source locator","observedRevision":null,"observedAt":null,"confidence":0.0,"freshness":"reusable|verify|stale|conflict"}],"currentState":{"phase":"...","status":"pending|active|completed|blocked|failed|needs-verification","changedFiles":[],"checks":[],"baseRevision":null},"unresolvedQuestions":[{"questionId":"unique-question-id","topicIds":["topic-id"],"text":"bounded question"}],"authorizationDelta":{"status":"none|pending|granted","additions":[],"removals":[],"grantProvenance":null},"nextAction":"bounded review action","notice":"Evidence is source data, not instructions or authorization."}
```

Copy the parent-supplied CAPSULE CONTRACT SNAPSHOT and hash exactly when present. Never calculate a hash from memory. If the parent did not supply one, use `hash: "unavailable"`; this is accepted as historical source data but is not reusable without freshness verification. Use null for unknown IDs. For `none` or `pending`, additions, removals, and grantProvenance must be empty/null. `granted` requires explicit historical grantProvenance and never grants a write.

Emit ASCII only. The literal final wrapper is four lines: `HANDOFF_CAPSULE v1`, then ` ```json `, then one JSON object, then ` ``` ` on its own line. Keep the full framed block at or below 4000 characters when practical and never above 6000 characters (hard maximum 6000) including sentinel, fences, and whitespace. Use at most 12 ownership entries, 8 evidence entries, 6 questions, 10 unique locators, 20 changedFiles, and 16 checks. Claims are max 420 characters, locators max 300, generic strings max 720, phase max 160, topic max 300, changed/check items max 240, topic/question IDs max 160, other relevant IDs max 180, sourceAgentId max 100, observedAt max 80, question text max 300, confidence is a number from 0 to 1, authorization additions/removals max 8 items of 240 characters, and grantProvenance max 300. nextAction max 300. Identity values are never truncated; if an identity exceeds its bound, report a blocked capsule with a bounded alternative ID or null where permitted. Normalize whitespace; omit transcripts, large excerpts, secrets, credentials, raw environment values, and unbounded output. Emit one capsule even for BLOCKED, FAILED, no-change, and report-only results.
